# 후속 구현 작업용 독립 프롬프트

아래 블록은 이후 별도 구현 요청에서 사용하는 프롬프트다. **이번 분석 작업에서 실행하지 않는다.** 실행 권장: XHigh. 모델·추론 설정은 작업 생성 시 지정하고 이 문서가 자동으로 변경하지 않는다.

```text
작업 대상은 game-algorithm-lab 저장소의 Procedural/LevelGeneration이다. 배정된 작업 디렉터리에서 진행하라. 원본 저장소 위치는 E:\Project\game-algorithm-lab이지만 원본을 직접 수정할 필요는 없다. 이번에는 조사 결과를 바탕으로 실제 구현을 요청한다.

먼저 현재 git 상태와 루트/프로젝트 AGENTS.md를 직접 확인한다. 기존 사용자 변경을 보존하고 대상 프로젝트에 집중한다. 합리적인 가정으로 끝까지 진행하며 불필요한 승인 질문을 하지 않는다.

목표
여러 형태의 건물·도로·시설을 공통 점유/노출/지지/경계/접근 관계로 설명하고, 직접 편집 후 일관되게 재생성되도록 개선하라. 영상별 장면 복제, 특정 건물 이름이나 좌표에 대한 분기, 예제 전용 알고리즘을 만들지 않는다. 입력 의도와 파생 계획을 분리하고 동일 관계에 동일 규칙을 적용한다.

필수 자료
1. docs/VIDEO_RULE_STUDY.md와 연결된 video-rule-study/OBSERVATIONS.md, RULE_SPEC.md, CODE_GAP_PLAN.md, HYPOTHESES.md, ACCEPTANCE.md를 읽는다. 캡처는 video-rule-study/evidence/gallery.html과 capture-log.json에서 확인한다.
2. docs/BUILDING_RULES_PORTING.md, PORTING_CONTRACT.md, NATIVE_PORTING.md, COMPLETE_FACE_ASSETS.md의 현재 계약을 읽고 실제 코드와 대조한다.
3. 분석 기준은 65701664aa9c4c4bb80320d58550e610511ea2b0이다. 현재 코드가 달라졌으면 차이를 먼저 반영한다. 오래된 ARCHITECTURE_PLAN/ENVIRONMENT_IMPLEMENTATION의 schema5 표기를 현재 기준으로 사용하지 않는다.

관찰 근거의 성격
15개 영상 모두 실제 플레이어 프레임을 확인했지만 연속 전편/모든 조작을 추적한 것은 아니다. 영상 내부 알고리즘은 미확정이다. 관찰 O01–O15, 가설 H01–H10, 제안 R01–R10, 코드 C01–C12를 구분한다. 물리 하중 지지, 실제 층간 이동, 차량 운행 가능성, seed/편집 순서 안정성, 신호등 자동 선택은 영상만으로 입증되지 않았다. 캡처의 주황색 선택과 실제 소스 명령을 혼동하지 않는다.

핵심 구현 범위
- P1/P2를 중심으로 기존 분석을 연결하는 실행 중 읽기 전용 공통 관계 뷰를 구현한다. 기존 SurfaceRegion, MassRelations, SpatialAnalysis, VerticalPlan을 재사용하고, supportOwner/실제 노출면/덮임/경계/frontage/sourceRefs/읽기 의존성 중 실제 소비하는 정보만 정리한다.
- 이 정보를 시설 선택·지지 검증과 기존 Inspector/진단에 실제로 연결한다. 사용하지 않는 추상 그래프만 추가하고 끝내지 않는다. 기존 로직이 이미 만족하는 행동은 보존하고 중복 구현하지 않는다.
- 낮은 날개와 높은 탑, L/U/구멍/덮인 테라스/돌출부/기둥 아래 공간에서 관계 판정이 소비자마다 엇갈리지 않도록 한다. 빈 셀을 AABB로 메우거나 억지 지지대·계단을 생성하지 않는다.
- 오브젝트 editor가 의도를 직육면체 fragment로 나누어도 같은 절대 칸의 위치와 문맥 의미를 유지한다. 한 칸 삭제가 무관한 칸의 모델을 이동시키지 않게 한다. 식생·외벽 시설·일반 시설의 지지 실패와 선택/탈락 이유를 일관되게 설명한다.
- 기존 도로 module/port와 spatial frontage/교차부의 관계를 필요한 소비자에 연결한다. 교차부 또는 도로 단절 후 영향을 받는 접근/시설을 누락하지 않는다.
- 먼저 기존 의미 출력을 보존하는 관계 연결을 완성하고, 새 동작이 필요하면 입력 조건과 기대 출력을 별도로 명세한 뒤 구현한다. 새 신호등·안테나 등 외형 확장은 기존 카탈로그에 없는 것으로 확인됐지만 핵심 구현 완료의 필수 항목은 아니다. H05/H08/H09를 원본의 확정 규칙으로 코딩하지 않는다.

단계
0. 현재 코드와 분석의 차이를 짧게 정리하고 보존/새 행동/범위 밖 목록을 구체화한다.
1. 기존 파이프라인 안에 최소 관계 인덱스와 sourceRefs/진단 연결을 넣는다. 영구 캐시나 문서 schema 필드를 먼저 추가하지 않는다.
2. 관련 시설·경계·접근 소비자를 연결하고 다양한 형태와 편집으로 확인된 불일치를 수정한다. 기본 정답 경로는 전체 재평가다.
3. 필요한 에셋/Viewer/Inspector 변경을 연결하되 규칙은 core에 둔다. geometry가 바뀌면 실제 bounds·예약·소유를 함께 검증한다.
4. ACCEPTANCE의 변경 관련 시나리오, 기존 보존, 필수 검사와 실제 편집 검증을 완료하고 구현/검증/제외 범위를 문서화한다.

보존 계약
- 현재 schema6 / environment-plans-v1 / SceneInputs2 / catalog13과 schema5 읽기 호환, 기존 사용자 rule/style/adapter 검증. 새 입력이 필요하지 않으면 버전 변경하지 않는다.
- Y-up 정수 셀, ±1,000,000, 축 span32, 방향/숫자 좌표/ASCII 동점, Box16 min 포함/max 제외, unsigned32 h33와 음수 modulo.
- 보존 anchor, 이전 성분 부피/좌표 순서의 분리·병합 상속, 절대 좌표 슬롯과 독립 결정 해시. 완전한 입력이 같으면 출력·순서가 같다. 최종 grid만 같고 anchor가 다르면 같은 입력으로 취급하지 않는다.
- 원본 점유와 빈 공간, 국소 비다양체 fallback, 구조면 단일 소유, A–D의 기존 층 배분·창/코너/옥상/사용자 타일 우선순위. C 지역 scope를 A/B/D에 무단 적용하지 않는다.
- 원본 기반 preflight, 실제 bounds 포함 검사, 공공900→차량800→입구700→구획600→시설500/400/300→마감200, 고정solid1000, 원자적 batch와 인증 crossing. 늦게 온 높은 우선순위 후보가 임의로 기존 예약을 추방하게 바꾸지 않는다.
- 보행 body/sweep, 가짜 수직 연결 금지, grid-car-v1과 주차 예산/proof. 개방형 주차 건물 외관과 실제 지상 주차 증명을 구분한다.
- 명시 시설 종류, 한 칸별 배치, 지지 상실 입력 보존, 미검증 접근 상태의 정직한 표기. 식생은 현재 이미 전셀 solid 예약에 참여한다.
- Undo/Redo/JSON/no-op/마지막 수락 문서와 화면 유지. 일반 건물 면당 실제 Mesh 하나에 채택 마감 포함, geometry/material 공유, 표시 원점과 문서 좌표 분리.
- 기존 214입력/419메시 보존 baseline을 새 결과로 갱신해 차이를 숨기지 않는다. 기존 공개 import/호출 계약과 등록 규칙 실행 경로를 유지한다.

범위 밖
새 세부 파라미터 편집 UI(용도·건물별 seed·수직비율·재료·bay 등의 임의 노출), 날씨/후처리/물 시뮬레이션, 지형 편집 체계, 내부 방 생성, 하중 solver, 실제 엘리베이터/계단 이동, 상층 주차 경사로/교통 시뮬레이션, 근거 없는 증분 scheduler는 추가하지 않는다. 기존 타일 편집 기능은 보존한다.
이번 구현은 엔진 이식 요청이 아니다. core에 DOM/Three.js를 넣지 않는다. 향후 이식에서도 JS 실행 브리지(ClearScript/V8/Node/WebView/JS WASM)나 원본 generator subprocess/서버를 런타임 의존성으로 도입하지 않는다.

미확정 사항 처리
원본 영상의 자동 지지·신호등·시설 종류 수식은 구현 차단 요건으로 삼지 않는다. 현재 보존 정책을 기본으로 선택하고 제안 설계라는 사실을 기록한다. 원본과 동일한 숨은 동작이 필요해졌을 때만 HYPOTHESES에 제시한 통제 편집 녹화/입력 로그로 확인한다. 원본 알고리즘을 복원했다고 주장하지 않는다.

인수 조건
- 연결된 관계 뷰를 실제 생성/검증·Inspector가 사용하며, 장면 ID/특정 좌표별 예외가 없다.
- L/U/구멍/단차/돌출부·지상/옥상/외벽·도로 유무와 단절·시설 한 칸 삭제에서 규칙이 성립한다. ACCEPTANCE T03–T12의 관련 양성/음성 사례를 검증한다.
- 동일 완전 입력의 순열/반복/캐시 on-off, 병합·분리 anchor, 편집 실패/Undo/Redo/저장복원의 보존을 확인한다. 기존 A–D 의미·geometry 보존 차이를 설명한다.
- 공유 규칙 변경 시 npm run verify를 완료한다. Viewer/편집 변경은 관련 실제 브라우저 경로와 스크린샷으로 확인한다. 이식 자료를 변경했다면 해당 AGENTS의 export/verify·Python 검사까지 수행한다.
- 실제 편집 비용과 기능 한계를 구분해 보고한다. 기존 통과 검사를 막연히 반복하거나 불필요한 범위 밖 리팩토링을 하지 않는다.
- 최종 보고에는 변경점, 실제 실행한 검사와 결과, 미수행한 중요한 검사, 남은 미확정 가설을 간결하게 적고 문서 링크를 제공한다.
```
