# 지상 주차 배치 재구현

2026-09-25. 기존 진단 기준은 `abb1d0318226b4913c78679db8c8008289ddf0c2`이며 [이전 분석](PARKING_CURRENT_STATE.md)과 그 증거는 그대로 보존했다.

첨부한 상업시설 주차장 사진의 반복 주차열·연결 차로·열 끝 조경 섬, 평면도의 보행 통로·횡단보도·ㄱ자 구간별 방향을 지상 마스크 생성 규칙에 반영했다. 원본 마스크·ID·anchor와 문서 형식은 유지한다.

## 생성과 선택

1. 도로와 solid를 제외한 실제 셀에서 폭 4칸 차로의 연속 구간을 찾는다. 부지 단면이 달라지는 곳에서도 구간을 나누어 ㄱ자 날개의 독립 배치를 만들 수 있게 한다.
2. 각 구간 양쪽에 깊이 2칸 구획과 뒤쪽 보행 접근이 들어가는지 검사한다. 한쪽만 가능한 좁은 구간은 한쪽 주차열을 만든다. 서로의 차로·구획·보행 공간을 침범하지 않는 주차열들을 조합한다. 정형 부지를 위한 반복 띠 후보도 함께 비교한다.
3. 도로 출입 후보를 첫 4개에서 자르던 제한을 없애고, 가장자리 안쪽과 서로 떨어진 위치부터 시험한다. 임의의 다른 도로 접점으로 출입하는 연결은 금지한다.
4. 출입구와 차로 구간을 차량 회전 sweep으로 연결한다. 도달하지 못하는 구획은 진입·출차 증명에서 탈락한다. 부지 밖은 차량 공간으로 확장하지 않는다.
5. 보행·차량 공간의 교차는 기존 직사각형 crossing 인증을 통과해야 한다. 선택적인 내부 보행 띠를 연결 차로가 끊으면 해당 부분을 제거하고 보행 접근을 다시 검사한다. 기존 공용 보행 예약은 제거할 수 없다.
6. 각 후보에 **최종 구획과 동일한 진입·출차·보행 검증**을 적용하고 실제 통과 대수로 선택한다. 후보 구획 수는 가지치기 상한으로만 사용한다. 시험 비용은 차로 탐색 예산에 청구하여 최종 구획 검증 예산을 보존한다.
7. 두 번째 출입구는 같은 도로변에도 가능하다. 실제 주차 대수를 늘리거나, 대수를 유지하면서 출차 전이를 줄일 때만 채택한다. 최대 2개이며 모든 추가 시도는 같은 부모 예산을 사용한다.
8. 건물 출입 계획을 반영한 뒤 구획을 최종 검증한다. 남은 열 끝 공간에만 조경 섬을 예약한다. 차로·보행·구획·건물 출입과 겹치는 섬은 생성하지 않는다.

계산은 [parking-layout.ts](../src/core/parking-layout.ts), [parking-circulation.ts](../src/core/parking-circulation.ts), [parking-stalls.ts](../src/core/parking-stalls.ts)에서 수행한다. Three.js 없이 실행되는 core 구조를 유지한다.

## 보행, 표시와 결과 계약

- 폭 6칸처럼 내부에 별도 보행 띠까지 넣기 어려운 경우, 바로 뒤의 **실제 비어 있는 지면**을 보행 공간으로 사용할 수 있다. `SpatialAnalysis.walkNodes`와 예약 충돌·연결 검증을 통과해야 하며 다른 주차 영역이나 solid를 통과할 수 없다. 이 외곽 보행 포장은 결과에 표시되지만 원본 주차 마스크에 셀을 추가하지 않는다.
- 다른 구획을 비워 둔다고 가정하지 않는다. 차량 경로는 도로·확정 차로·자기 구획 두 셀만 사용한다. 보행 검증도 다른 주차 셀을 임의의 통로로 쓰지 않도록 제한한다.
- 차로는 어두운 포장, 보행 공간은 밝은 포장으로 표시한다. 승인 구획의 U자 경계선, 인증된 횡단보도, 출입 및 양방향 차로 화살표, 둥근 경계석의 낮은 조경 섬을 표시한다.
- `layoutId`는 선택된 배치 후보를 식별한다. `axis`는 주 방향이며, 혼합 배치의 실제 각 열 방향은 구획의 `heading`을 따른다. 혼합 배치의 `periodCells`는 0이다.
- `search`에 출입 후보 수, 생성된 배치 조합 수, 평가·가지치기·미시도 수와 검증 대수를 기록한다. `complete`는 **생성한 후보 집합**에 대한 완료 여부이지 전역 최적성 증명이 아니다. 예산 미배정도 완료로 표시하지 않는다.
- 탐색 단계의 `search.provenStalls`는 이후 건물 출입 예약에 의해 최종 대수보다 클 수 있다. 사용자 요약은 최종 `quality.acceptedStalls`를 사용한다.
- `ParkingPlan.islands`와 `quality.islandCells`를 추가했다. 면적 합계는 `차로 + 보행 전용 + 구획 + 조경 섬 + 미배정 = 유효 셀`이다.
- 기존 `untestedStalls`는 선택된 구획 집합에 관한 값이다. 사용자 요약은 이를 전체 탐색 완료처럼 표시하지 않고, 0대인 경우와 탐색 한도를 명시한다.

## 이전 구현과 같은 입력 비교

| 사례 | 이전 검증 대수 | 새 검증 대수 |
|---|---:|---:|
| 12×12 | 18 | 18 |
| 12×12 중앙 한 칸 제거 | 0 | 17 |
| ㄱ자 16×16, 날개 8 | 19 | 26 |
| U자 16×16, 날개 6 | 7 | 27 |
| 폭 6×길이 16 | 4 | 13 |
| 좁은 목으로 연결된 두 구역 | 18 | 32 |
| 30×20 | 96 | 99 |
| 30×20 내부 장애물 | 86 | 93 |

[27개 입력의 실행 결과](parking-reimplementation-evidence/report.json). 표는 현재 고정 격자 차량 모델의 검증된 용량이며 전역 최댓값이 아니다. 후보별 증명으로 계산량이 늘었다. CPU 표본은 단일 uncached 실행이므로 성능 분포나 지연 상한을 의미하지 않는다. 예산은 기존 영역당 차로 1,500,000, 최종 구획 500,000, 시도 128을 유지한다.

차량 모델은 기존 `grid-car-v1`(1×2 footprint, 4×4 회전 sweep)을 유지했다. 이번 구현은 지상 평면 배치이며 다층 주차 건물의 경사로·층간 주행은 포함하지 않는다.

## 검증

- `npm run verify`: 타입 검사, 단위 테스트 360개, 프로덕션 빌드 통과.
- 회귀 테스트는 중앙 한 칸 제거, 혼합 방향 ㄱ자, U자, 좁은 한쪽 열, 두 출입구, 외곽 보행 장애물, 입력 순서 결정론을 검사한다. 모든 구획·섬을 점유한 후 보행 연결과 각 차량 경로를 다시 검사한다.
- 기존 차로·구획·품질·cache 동등성 검사를 유지했다. 두 번째 출입구 검사는 강제로 2개를 요구하는 대신 용량 감소 거절과 채택 조건을 검사한다. 역사적 보존 baseline은 수정하지 않았다.
- Chromium 브라우저 테스트 9개 통과. 실제 클릭/Q로 중앙 한 칸을 제거했을 때 17대가 유지되며 Undo/Redo 및 JSON 저장·재불러오기 결과도 일치했다. 별도 화면 증거의 브라우저 pageerror는 0건이다.
- 전체 성능 반복 측정(`@measure`)과 다층 주행 검증은 수행하지 않았다. 이번 변경의 확인 범위는 지상 배치·증명·표시·편집이다.

```powershell
node docs/parking-reimplementation-evidence/probe.mjs
node docs/parking-reimplementation-evidence/browser.mjs
npx playwright test e2e/parking-layout-v2.spec.ts e2e/environment.spec.ts --grep-invert '@measure'
```

[ㄱ자 혼합 주차열](parking-reimplementation-evidence/L16.png) · [U자 부지](parking-reimplementation-evidence/U16-arms6.png) · [대형 반복 주차열](parking-reimplementation-evidence/R30-iso.png) · [한 칸 제거 후](parking-reimplementation-evidence/R12-single-hole.png).

앱의 JSON 불러오기로 확인할 수 있는 입력: [ㄱ자 부지](parking-reimplementation-evidence/L16.json), [30×20 부지](parking-reimplementation-evidence/R30.json).
